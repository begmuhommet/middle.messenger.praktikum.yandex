import { Routes } from "../../../routes";

const mainData = {
  links: [
    { link: "Login", href: Routes.Login },
    { link: "Registration", href: Routes.Register },
    { link: "Chat", href: Routes.Chat },
    { link: "Error", href: Routes.Error },
  ],
};

export default mainData;
